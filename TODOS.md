# Follow-ups

- Add Decode Lab session history keyed by payload hash, idlHash, programId, and codeId after core decode is stable.
- Add read-only encode and snippet helpers for `SailsService.encodePayload` and constructor `fromCodeId` shapes after decode UX lands.
- Add live auto-decode from block/message surfaces after raw payload extraction is reliable enough to avoid fake coverage.
- Treat broader ecosystem-stage claims from the draft spec as deferred until they are tested against real Sails/Vara program, code, message, and IDL flows.
- Add richer mobile Decode Lab tabs or accordions with persistent source readiness and Decode action after the desktop-first workbench settles.
